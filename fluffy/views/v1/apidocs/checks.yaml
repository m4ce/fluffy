Retrieve the active rollback checks
---
tags:
  - checks
parameters: []
responses:
  200:
    description: The list of active rollback checks
  500:
    description: There was an issue in accessing the active rollback checks
    schema:
      id: CheckError
      properties:
        message:
          description: API response
          type: string
        error:
          description: Error message
          type: string
